import random


"""random模块"""

# random.random  随机产生一个0-1之间的浮点数
#
# random.randint(a,b）  随机生成[a,b]之间的一个整数
#
# random.randrange(a,b,step)  随机产生的数之间间隔是step,也就是等差
#
#
# random.sample(range(10),2)  从range(10)中随机产生2个数的列表
#
# random.choice(sequence)  从特定序列中随机产生一个元素，这个序列可以是元组，列表，字符串
